<NoUsersFound>:
    width: self.texture_size[0]
    height: self.texture_size[1]
    size_hint: None, None
    pos_hint: {'left': 1}
    font_size: '16sp'
    text: 'no users found'


<SearchResult>:

    size_hint: 1, None
    height: app.search_result_record_height
    padding: 0
    spacing: 1

    RaisedIconButton:
        icon: 'account-plus'
        button_width: app.btn_icon_small_width
        button_height: app.btn_icon_small_height
        font_size: '16sp'
        on_press: root.on_search_result_add_clicked()

    HFixedEmptySpace:
        width: dp(5)

    HFlexMarkupLabel:
        id: search_result_label
        pos_hint: {'left': 1, 'center_y': 0.5}
        font_size: '16sp'
        text: ''


<SearchPeopleScreen>:

    VerticalLayout:
        size_hint: 1, 1
        pos_hint: {'top': 1}
        padding: 15

        HorizontalLayout:
            size_hint: 1, None
            height: self.minimum_height

            CustomTextField:
                id: search_input
                size_hint_x: None
                width: dp(220)
                hint_text: 'Enter user name to search'
                text: ''
                on_focus: root.on_search_input_focus_changed()

            VerticalLayout:
                size_hint: None, None
                width: self.minimum_width
                height: self.minimum_height

                RaisedIconButton:
                    id: search_button
                    _radius: dp(3)
                    icon: 'search-web'
                    on_press: root.on_search_button_clicked()

                Widget:
                    size_hint: None, None
                    size: dp(16), dp(16)

        VerticalScrollView:

            VerticalLayout:
                id: search_results
                size_hint: 1, None
                height: self.minimum_height
                pos_hint: {'top': 1}
                padding: 5
                spacing: 5
