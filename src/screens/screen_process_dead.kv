#:import ScreenContainerLayout components.layouts.ScreenContainerLayout
#:import MarkupLabel components.labels.MarkupLabel

<ProcessDeadScreen>:

    ScreenContainerLayout:

        TopEmptyScreenSpace:

        VLayout:
            size_hint: 1, 1

            VEmptySpace:
                size_hint_y: 0.5

            HLayout:
                size_hint: 1, .01

                HEmptySpace:
                    size_hint_x: 0.5

                MarkupLabel:
                    id: deploy_output_label
                    text: '[size=26sp]starting BitDust[/size]\n'

                HEmptySpace:
                    size_hint_x: 0.5

            VEmptySpace:
                size_hint_y: 0.5
