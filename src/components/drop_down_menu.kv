<BaseFloatingRootButton>:
    theme_text_color: "Custom"
    md_bg_color: self.theme_cls.primary_color

    canvas.before:
        PushMatrix
        Rotate:
            angle: self._angle
            axis: (0, 0, 1)
            origin: self.center
    canvas.after:
        PopMatrix


<BaseFloatingBottomButton>:
    theme_text_color: "Custom"
    md_bg_color: self.theme_cls.primary_color

    canvas.before:
        Color:
            rgba:
                self.theme_cls.primary_color \
                if not self._bg_color else self._bg_color
        RoundedRectangle:
            pos:
                (self.x - self._canvas_width + dp(1.5)) + self._padding_right / 2, \
                self.y - self._padding_right / 2 + dp(1.5)
            size:
                self.width + self._canvas_width - dp(3), \
                self.height + self._padding_right - dp(3)
            radius: [self.height / 2]


<BaseFloatingLabel>:
    size_hint: None, None
    padding: "8dp", "4dp", "8dp", "4dp"
    height: label.texture_size[1] + self.padding[1] * 2
    width: label.texture_size[0] + self.padding[0] * 2
    elevation: 10

    canvas:
        Color:
            rgba:
                self.theme_cls.primary_color \
                if not root.bg_color else root.bg_color
        RoundedRectangle:
            pos: self.pos
            size: self.size
            radius: [5]

    Label:
        id: label
        markup: True
        text: root.text
        size_hint: None, None
        size: self.texture_size
        color: root.theme_cls.text_color if not root.text_color else root.text_color


<CustomFloatingRootButton>:
    icon: 'dots-vertical'
    text_color: app.color_white99
    md_bg_color: self.theme_cls.primary_dark
    user_font_size: "17sp"
    padding: "0dp"


<CustomFloatingBottomButton>:
    text_color: app.color_white99
    md_bg_color: self.theme_cls.primary_color
    _bg_color: self.theme_cls.primary_light
    user_font_size: "17sp"
    padding: "0dp"


<CustomFloatingLabel>:
    text_color: app.color_white99


<CustomFloatingActionButtonSpeedDial>:
    # label_text_color: app.color_white99
    # color_icon_stack_button: app.color_white99
    # color_icon_root_button: app.color_white99
    # bg_color_root_button: self.theme_cls.primary_dark
    # bg_color_stack_button: self.theme_cls.primary_color
    # bg_hint_color: self.theme_cls.primary_light
