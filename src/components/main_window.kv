<MainWindow>:

    canvas.before:
        Color:
            rgba: app.color_white
        Rectangle:
            pos: self.pos
            size: self.size

    color: app.color_black

    BoxLayout:
        orientation: 'vertical'
        size_hint: 1, 1
        padding: 0
        sizing: 0

        StackLayout:
            id: nav_buttons_layout
            orientation: 'lr-tb'
            padding: 0, 0
            spacing: 1, 1
            size_hint: 1, None
            height: self.minimum_height

	        MDIconButton:
	            id: main_nav_button
	            disabled: True
	            icon: 'src/images/logo-color-28x28.png'
	            padding: (0, 0)
	            pos_hint: {"center_x": .5, "center_y": .5}
	            size_hint: None, None
	            size: (app.nav_height, app.nav_height)
	        	on_press: root.select_screen('main_menu_screen', verify_state=True)

        ScreenManagement:
            id: screen_manager
            size_hint: 1, 1
            pos_hint: {'top': 1}
